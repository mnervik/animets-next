generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider     = "mysql"
  url          = env("DATABASE_URL")
  relationMode = "prisma"
}

model Video {
  id   Int    @id @default(autoincrement())
  name String @unique

  @@map("video")
}
